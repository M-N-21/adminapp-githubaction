version: '3'
services:
  app:
    image: mn21/admin-app:latest
    ports:
      - "8081:8081"
    depends_on:
      - db
  db:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD:
      MYSQL_DATABASE: adminapp-db
